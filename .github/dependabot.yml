---
version: 2
updates:
    - package-ecosystem: pip
      directory: /backend/app # Location of package manifests
      schedule:
          interval: weekly

    - package-ecosystem: docker
      directory: /backend
      schedule:
          interval: weekly
